package:
  name: rachis
  version: {{ PLUGIN_VERSION }}
source:
  path: ..
build:
  script: make install
requirements:
  host:
  - python {{ python }}
  - setuptools
  - versioningit
  - wheel
  run:
  - python {{ python }}
  - pyyaml
  - decorator {{ decorator }}
  - pandas {{ pandas }}
  - tzlocal {{ tzlocal }}
  - python-dateutil
  - bibtexparser
  - networkx {{ networkx }}
  - dill
  - psutil
  - flufl.lock
  - parsl {{ parsl }}
  - appdirs
  - tomlkit
  - lxml
  build:
  - python {{ python }}
  - setuptools
  - versioningit
test:
  requires:
  - pytest
  - tornado
  - notebook {{ notebook }}
  imports:
  - rachis
  commands:
  - QIIMETEST= python -c "import rachis.plugins.dummy_plugin"
  - QIIMETEST= py.test --pyargs --doctest-modules rachis
about:
  home: https://qiime2.org
  license: BSD-3-Clause
  license_family: BSD
